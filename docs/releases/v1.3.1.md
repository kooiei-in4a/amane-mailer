# Release evidence — v1.3.1

> Status: **PUBLISHED**
>
> Target: `1.3.1`

## Release boundary

- Major release concern: Issue #622, Shared Qualification Artifact Contract.
- Scope: Release Engineering hardening only, on the .NET `10.0.11` servicing
  baseline with SDK `10.0.303`.
- No product feature, runtime semantic change, database migration, or API shape
  change is included.
- The migration inventory remains the existing `001` through `018`.
- Mailpit qualification requires a digest-pinned input.

## Published image evidence

- Workflow: [Publish Release Image run #32634626532](https://github.com/kooiei-in4a/amane-mailer/actions/runs/32634626532)
- Source commit SHA: `6ffd1b3887f69d5aca407193903ea241c752daf7`
- Platform: `linux/amd64`
- Published digest: `sha256:c3596d5c20b2f73fc4e9c4593be99e0e1d484bab032293ef8df8017841e50f06`
- Tags:
  - `ghcr.io/kooiei-in4a/amane-mailer:v1.3.1`
  - `ghcr.io/kooiei-in4a/amane-mailer:sha-6ffd1b3887f69d5aca407193903ea241c752daf7`
- Build smoke: PASS
- No-cache reproducibility: PASS; the same digest was produced
- Public consumer verification: PASS; both tags resolved to the published digest,
  the digest image pulled as `linux/amd64`, OCI labels matched, and `--help` succeeded
- `latest` was not published.

The machine-readable evidence schema is implemented for subsequent publish
runs in the `publish-release-image-evidence-<sha>-<run-id>` artifact. The
supplied v1.3.1 run predates this automation; its public verification is
recorded above, and no re-publication or workflow dispatch was performed.

## Qualification authority

- Candidate `releaseVersion`: `1.3.1`
- v1.3.0 authority: `v1.3.0-rc-qualification` / Issue #583 (historical, unchanged)
- v1.3.1 authority: `v1.3.1-rc-qualification` / Issue #622
- `scopeVersion`: `1`
- The v1.3.1 scope is an explicit overlay that removes exactly the eight
  G456 Windows-Docker Hard variants; it does not rewrite or reuse the RC2 run.
- v1.3.2 and later releases are not implicitly approved by either authority.

## RC2 qualification boundary

- RC2 exact commit: `6f0865aa394c3363ed8f5671e3f423453d3da6b8`
- RC2 stopped at `13` active Hard PASS with `0` active FAIL.
- RC2 remains valid, unsealed, and immutable historical evidence.
- The eight G456 Windows-Docker Hard variants were deliberately skipped; under
  the historical scope, the run therefore could not become `GO_ELIGIBLE`.
- A revised v1.3.1-specific qualification scope is introduced for a future RC3.
- There is no product, runtime, API, or migration semantic delta in this
  scope revision.

## Dogfood path

```text
immutable qualification artifact
  -> prepare-qualification-handoff.py
  -> exact producer identity
  -> exact file set
  -> symlink rejection
  -> sealed-only byte-identical view
  -> unchanged strict sealed validator
  -> Git / OCI consumers
```

The preparation helper does not mutate the immutable input, and the existing
strict sealed validator is neither changed nor relaxed.

## Historical RC1

- Branch: `release/v1.3.1-rc1`
- Commit: `11b473d802e33bee00dc77cb58e47e4a7fef8763`
- Disposition: **NO CANDIDATE / VERSION ALIGNMENT STOP**

RC1 is an immutable historical no-go branch. No candidate was generated from it.

## Historical qualification identities

The RC3 qualification, candidate, promotion, package, and GitHub Release
identities above remain historical preparation fields and are not inferred from
the minimal v1.3.1 image publication. The published OCI identity is the source
SHA and digest recorded in the section above; no additional publication action
is required for v1.3.1.
