# Release evidence — v1.3.1

> Status: **RELEASE PREPARATION — NOT YET PUBLISHED**
>
> Target: `1.3.1`

## Release boundary

- Major release concern: Issue #622, Shared Qualification Artifact Contract.
- Scope: Release Engineering hardening only, on the .NET `10.0.11` servicing
  baseline with SDK `10.0.303`.
- No product feature, runtime semantic change, database migration, or API shape
  change is included.
- The migration inventory remains the existing `001` through `018`.
- Mailpit qualification requires a digest-pinned input.

## Qualification authority

- Candidate `releaseVersion`: `1.3.1`
- Scope authority origin: `v1.3.0-rc-qualification`
- `scopeVersion`: `1`
- Authority issue: #583
- The checked-in v1.3.0 scope manifest remains unchanged and is explicitly reused
  as a patch-compatible authority for v1.3.1.
- v1.3.2 and later releases are not implicitly approved by this authority reuse.

## Dogfood path

```text
immutable qualification artifact
  -> prepare-qualification-handoff.py
  -> exact producer identity
  -> exact file set
  -> symlink rejection
  -> sealed-only byte-identical view
  -> unchanged strict sealed validator
  -> Git / OCI consumers
```

The preparation helper does not mutate the immutable input, and the existing
strict sealed validator is neither changed nor relaxed.

## Historical RC1

- Branch: `release/v1.3.1-rc1`
- Commit: `11b473d802e33bee00dc77cb58e47e4a7fef8763`
- Disposition: **NO CANDIDATE / VERSION ALIGNMENT STOP**

RC1 is an immutable historical no-go branch. No candidate was generated from it.

## Pending release identities

Future identities are not inferred or invented during release preparation:

- RC2 exact `releaseCommitSha`: **PENDING**
- Candidate workflow run / attempt: **PENDING**
- Candidate artifact IDs: **PENDING**
- `candidateId`: **PENDING**
- OCI digest: **PENDING**
- `bindingId`: **PENDING**
- `qualificationRunId`: **PENDING**
- `sealedEventId`: **PENDING**
- Promotion PR: **PENDING**
- Final `main` merge: **PENDING**
- Tag: **PENDING**
- NuGet package identity: **PENDING**
- GitHub Release assets: **PENDING**

No candidate, qualification, promotion, tag, package publish, OCI publish, or
GitHub Release action is performed by this preparation.
