# Amane Mailer v0.3.0 Release Record

Value-free release evidence for OSS consumers. No credentials, recipient addresses,
tokens, raw logs, or host-specific private paths.

This file is a pre-publish draft for the `v0.3.0` minor release. Fill the
placeholder values after the human release operator creates the tag, publishes
artifacts, and runs public smoke validation.

## Source

- Git tag: `v0.3.0` (not created in this release-prep PR)
- Annotated tag object: `TBD after tag creation`
- Commit: `TBD after release commit is selected`
- GitHub release: `TBD after GitHub Release draft/publication`
- Release PR: `TBD after release-prep PR is opened/merged`

## Included changes since v0.2.0

### First-mail quickstart / local onboarding

- Zero-Admin first-mail quickstart for a fresh local Mailpit setup (#145).
- Bash and PowerShell local first-mail smoke scripts for first-success checks
  (#146, #147).

### `payload_hash` verifier and troubleshooting

- Request JSON verifier for `payload_hash` mismatch self-diagnosis (#148).
- Troubleshooting guidance that distinguishes invalid payload hashes from
  idempotency conflicts (#149).

### Tenant config and environment preflight

- Tenant config / environment preflight for common startup mistakes (#150).
- Tenant troubleshooting matrix tied to preflight terminology and outputs (#151).

### Consumer POST examples

- Runnable .NET Consumer sample using `Amane.Mailer.Contracts` (#152).
- Runnable Python Consumer sample using the Python payload hash helper (#153).
- Runnable Node.js Consumer sample using the JavaScript payload hash helper
  (#154).

### Release docs / default tag consistency

- README, release smoke docs, release smoke scripts, supported-version policy,
  and drift checks aligned on `v0.3.0` defaults (#155, #173).
- CHANGELOG and release evidence draft prepared for v0.3.0 (#174).

The public `POST /internal/mail-requests` HTTP contract shape and acceptance
semantics are unchanged.

## Docker

- Image: `ghcr.io/kooiei-in4a/amane-mailer:v0.3.0`
- Index digest: `TBD after GHCR image publish`
- Runtime manifest digest (`linux/amd64`): `TBD after GHCR image publish`
- Runtime manifest digest (`linux/arm64`): `TBD after GHCR image publish`
- Attestation manifest digest (`linux/amd64`): `TBD after GHCR image publish`
- Attestation manifest digest (`linux/arm64`): `TBD after GHCR image publish`
- Immutable tag: `ghcr.io/kooiei-in4a/amane-mailer:sha-<release-commit>`
- Immutable tag digest: `TBD after GHCR image publish`
- Target: `linux/amd64` and `linux/arm64` (multi-arch index expected)
- OCI labels:
  - `org.opencontainers.image.source=https://github.com/kooiei-in4a/amane-mailer`
  - `org.opencontainers.image.revision=<release-commit>`
  - `org.opencontainers.image.version=v0.3.0`
  - `org.opencontainers.image.description=Amane Mailer runtime image`
- Publish workflow: `TBD after publish-image workflow run`

## NuGet

- Package: `Amane.Mailer.Contracts`
- Version: `0.3.0`
- Package URL: `TBD after NuGet publish`
- Listed: `TBD after NuGet publish`
- SourceLink / repository commit: `TBD after NuGet publish`
- Service release commit (`v0.3.0` tag on `main`): `TBD after tag creation`
- Publish workflow: `TBD after publish-contracts workflow run`
- Symbol package (.snupkg): `TBD after NuGet symbol verification`

## SDK

- .NET SDK: `10.0.301`
- Roll-forward: `latestPatch`

## Pre-publish validation

| Check | Result |
| --- | --- |
| `dotnet restore Amane.Mailer.slnx --locked-mode` | PASS on 2026-07-04 JST |
| `dotnet build Amane.Mailer.slnx -c Release --no-restore` | PASS on 2026-07-04 JST (0 warnings, 0 errors) |
| `dotnet test Amane.Mailer.slnx -c Release --no-build --verbosity minimal` | PASS on 2026-07-04 JST (296 passed, 0 failed) |
| `node scripts/validate-openapi.mjs docs/api/openapi.yaml` | PASS on 2026-07-04 JST (10 refs, 3 operations) |
| `node scripts/check-contract-drift.mjs` | PASS on 2026-07-04 JST |
| `node scripts/check-release-smoke-tag-drift.mjs` | PASS on 2026-07-04 JST |
| Native AOT publish smoke | PASS on 2026-07-04 JST via Docker local first-mail smoke build (`PublishAot=true`, `IlcTreatWarningsAsErrors=true`) |
| Local Docker / Mailpit smoke | PASS on 2026-07-04 JST via local first-mail smoke (5 passed, 0 failed) and release-smoke against local AOT image (8 passed, 0 failed) |
| Bash first-mail smoke | NOT RUN to completion on 2026-07-04 JST: WSL `HCS_E_CONNECTION_TIMEOUT` prevented `bash scripts/local-first-mail-smoke.sh` startup |
| PowerShell first-mail smoke | PASS on 2026-07-04 JST (`.\scripts\local-first-mail-smoke.ps1`, 5 passed, 0 failed) |
| `payload_hash` verifier / examples | PASS on 2026-07-04 JST for Python verifier/vectors and JavaScript vectors; Go example not run because `go` is not installed |
| Consumer examples | PASS on 2026-07-04 JST for .NET, Python, and Node.js samples (`202 Accepted`) against local Mailer |
| Tenant config preflight | PASS on 2026-07-04 JST for `config/mailer/tenants.example.json` with `MAIL_SERVICE_TOKEN=local-mail-service-token` |

## Release image smoke

Clean-state pull and automated smoke of the public release runtime path via
[`scripts/release-smoke.sh`](../../scripts/release-smoke.sh) or
[`scripts/release-smoke.ps1`](../../scripts/release-smoke.ps1). Runbook:
[Clean-state release image smoke](../ops/release-image-smoke.en.md).

| Field | Value |
| --- | --- |
| Status | `TBD after public release smoke` |
| Date | `TBD after public release smoke` |
| Image | `ghcr.io/kooiei-in4a/amane-mailer:v0.3.0` |
| Image digest | `TBD after GHCR image publish` |
| Environment | `TBD after public release smoke` |

| Check | Result |
| --- | --- |
| `GET /healthz` | `TBD after public release smoke` |
| `GET /readyz` | `TBD after public release smoke` |
| Valid `POST /internal/mail-requests` | `TBD after public release smoke` |
| Mailpit delivery | `TBD after public release smoke` |
| Idempotent repost (same id + payload) | `TBD after public release smoke` |
| Conflict (same id + different payload) | `TBD after public release smoke` |
| Invalid token | `TBD after public release smoke` |
| Invalid `source_service` | `TBD after public release smoke` |

Reproduce from the repository root after publish:

```bash
bash scripts/release-smoke.sh
```

Pin the immutable tag after publish:

```bash
MAILER_IMAGE_TAG=sha-<release-commit> bash scripts/release-smoke.sh
```

## Known limitations

- SQLite deployment assumes a single node / single replica.
- Admin UI remains disabled by default, internal-network-only, and experimental.
- `202 Accepted` means the request was persisted, not that provider delivery
  completed.
- Delivery is at-least-once. If the service stops after provider send succeeds
  but before the database records delivery, the same mail may be sent again.
- No webhook delivery result API.
- No template engine.
- No full multi-recipient support.

## Related evidence

- [v0.2.0 release record](v0.2.0.md)
- [Release notes checklist](../ops/release-notes-checklist.en.md)
- [GHCR image publish](../ops/ghcr-image-publish.en.md)
- [Release artifact verification](../ops/release-artifact-verification.en.md)
- [Zero-Admin first-mail quickstart](../ops/first-mail-quickstart.en.md)
- [payload_hash examples](../../examples/payload-hash/README.md)
