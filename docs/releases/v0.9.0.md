# Amane Mailer v0.9.0 Release Record

Value-free release evidence for OSS consumers. No credentials, recipient addresses,
tokens, raw logs, or host-specific private paths.

This file is the release record for the `v0.9.0` release. Tag, Docker, NuGet, and
the GitHub Release fields below stay pending until publish completes.

Public release numbering advances from `v0.4.0` to `v0.9.0` for this feature
bundle; there were no intermediate public releases between them.

## Source

- Git tag: `v0.9.0` (pending)
- Annotated tag object: TBD after tag object inspection
- Tag target commit: TBD after tag on `main`
- GitHub release: TBD after GitHub Release publication
- Docs / version prep PR: TBD (`feature/docs-0.9.0-prep` → `develop`)
- Main promotion PR: TBD (`develop` → `main`)

## Included changes since v0.4.0

### Consumer HTTP API

- Tenant-scoped delivery status GET (`#216`)
- Cancel and reschedule for scheduled / queued requests (`#220`)
- Optional `scheduled_at` on create; independent from retry `next_attempt_at`
- Signed delivery-result webhooks (`#219`)

### Ops

- Prometheus `/metrics` endpoint and runbook (`#217`)

### SDKs

- Official TypeScript and Python Consumer SDKs under `sdk/` (`#218`)

### Contracts / OpenAPI

- `Amane.Mailer.Contracts` and OpenAPI `info.version` aligned on `0.9.0`
- New / extended types: `MailRequestStatusResponse`, `MailDeliveryEventPayload`,
  `MailRequestRescheduleRequest`, `MailRequestScheduleLimits`, `scheduled_at`,
  schedule error codes

## Draft GitHub Release notes

Mirror the `CHANGELOG.md` 0.9.0 section. Keep publish-dependent artifact fields
pending until GHCR / NuGet publish completes.

### Artifact status

- Git tag / annotated tag object: pending
- Docker image URL / digest: pending
- NuGet package URL and symbol verification: pending
- Release-image smoke: pending recording under
  [Release image smoke](#release-image-smoke)

## Docker

- Image: `ghcr.io/kooiei-in4a/amane-mailer:v0.9.0` (pending publish)
- Immutable tag: `sha-<git-sha>` (pending)
- Platforms: `linux/amd64`, `linux/arm64` (pending manifest verification)

## NuGet

- Package: `Amane.Mailer.Contracts`
- Version: `0.9.0`
- Package URL: <https://www.nuget.org/packages/Amane.Mailer.Contracts/0.9.0>
  (pending publish)
- Symbol package (.snupkg): pending workflow push / indexing verification

## Release image smoke

Record value-free smoke results here after publish (digest, date, environment,
per-check pass/fail). Follow [release-image-smoke.md](../ops/release-image-smoke.md).

## Known limitations

- SQLite deployment assumes a single node / single replica.
- Admin UI remains disabled by default, internal-network-only, and experimental.
- `202 Accepted` means the request was persisted, not that provider delivery
  completed.
- Delivery is at-least-once. If the service stops after provider send succeeds
  but before the database records delivery, the same mail may be sent again.
- Webhook endpoints are optional per tenant (one URL per tenant in this release).
- Consumer SDK status polling helpers are not yet included (HTTP GET is available;
  SDK Phase 2 follow-up).
- No template engine.
- No full multi-recipient support.

## Related evidence

- [v0.4.0 release record](v0.4.0.md)
- [Release notes checklist](../ops/release-notes-checklist.en.md)
- [GHCR image publish](../ops/ghcr-image-publish.en.md)
- [Release artifact verification](../ops/release-artifact-verification.en.md)
- [Metrics and alerts](../ops/metrics-and-alerts.en.md)
- [Webhook verification](../consumer/webhook-verification.md)
