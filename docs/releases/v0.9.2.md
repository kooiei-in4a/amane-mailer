# Amane Mailer v0.9.2 Release Record

Value-free release evidence for OSS consumers. No credentials, recipient addresses,
tokens, raw logs, or host-specific private paths.

This file is the release record for the `v0.9.2` release. Tag, Docker, NuGet,
the GitHub Release, and public release-image smoke (`linux/amd64` and
`linux/arm64`) are recorded below after publish.

## Source

- Git tag: `v0.9.2` (pending)
- Annotated tag object: pending
- Tag target commit: pending
- GitHub release: pending
- Docs / version prep PR: pending (merged to `develop`)
- Main promotion PR: pending (merged to `main`)

## Included changes since v0.9.1

### Reliability / operations

- Fail fast on Worker / Webhook / Sweep / Retention / Healthcheck numeric
  misconfiguration instead of silent clamping (#329).
- Require metrics bearer token outside Development when metrics stay enabled
  (#283).
- Map provider delivery failures to a stable `error_code` taxonomy (#279).

### Observability

- Record a fixed primary `/readyz` failure reason via transition-only logs and
  Prometheus gauges `mail_ready` / `mail_readiness_failure` without changing the
  public HTTP contract (#330).
- Include structured `RequestId` and `TenantId` on Worker and
  ExpiredProcessingReaper request logs (#285).

### Admin hardening

- Enforce Admin PBKDF2 password-hash parameter bounds (#281).
- Parse Admin boolean and positive-integer environment variables strictly (#280).

### Contracts / OpenAPI

- `Amane.Mailer.Contracts` and OpenAPI `info.version` aligned on `0.9.2`
  (patch bump; no new consumer HTTP contract surface).

## Draft GitHub Release notes

Mirror the `CHANGELOG.md` 0.9.2 section. GitHub Release will be published at
<https://github.com/kooiei-in4a/amane-mailer/releases/tag/v0.9.2>.

### Artifact status

- Git tag / annotated tag object: pending
- Docker image URL / digest: pending
- NuGet package URL and symbol verification: pending
- Release-image smoke: pending (`linux/amd64` and `linux/arm64`)

## Docker

- Image: `ghcr.io/kooiei-in4a/amane-mailer:v0.9.2`
- Index digest: pending
- Runtime manifest digest (`linux/amd64`): pending
- Runtime manifest digest (`linux/arm64`): pending
- Attestation manifest digest (`linux/amd64`): pending
- Attestation manifest digest (`linux/arm64`): pending
- Immutable tag: pending (`ghcr.io/kooiei-in4a/amane-mailer:sha-<git-sha>`)
- Immutable tag digest: pending
- Target: `linux/amd64` and `linux/arm64` (expected multi-arch after publish)
- OCI labels: pending
- Publish workflow: pending

## NuGet

- Package: `Amane.Mailer.Contracts`
- Version: `0.9.2`
- Package URL: <https://www.nuget.org/packages/Amane.Mailer.Contracts/0.9.2>
- Listed: pending
- SourceLink / repository commit: pending
- Service release commit (`v0.9.2` tag on `main`): pending
- Publish workflow: pending
- Symbol package (.snupkg): pending

## Release image smoke

Clean-state pull and automated smoke of the public release runtime path via
[`scripts/release-smoke.sh`](../../scripts/release-smoke.sh) or
[`scripts/release-smoke.ps1`](../../scripts/release-smoke.ps1). Runbook:
[Clean-state release image smoke](../ops/release-image-smoke.en.md).

### `linux/amd64`

| Field | Value |
| --- | --- |
| Status | pending |
| Date | pending |
| Image | `ghcr.io/kooiei-in4a/amane-mailer:v0.9.2` |
| Image digest | pending |
| Environment | pending |

| Check | Result |
| --- | --- |
| `GET /healthz` | pending |
| `GET /readyz` | pending |
| Valid `POST /internal/mail-requests` | pending |
| Mailpit delivery | pending |
| Idempotent repost (same id + payload) | pending |
| Conflict (same id + different payload) | pending |
| Invalid token | pending |
| Invalid `source_service` | pending |

### `linux/arm64`

| Field | Value |
| --- | --- |
| Status | pending |
| Date | pending |
| Image | `ghcr.io/kooiei-in4a/amane-mailer:v0.9.2` |
| Image digest | pending |
| Environment | pending |

| Check | Result |
| --- | --- |
| `GET /healthz` | pending |
| `GET /readyz` | pending |
| Valid `POST /internal/mail-requests` | pending |
| Mailpit delivery | pending |
| Idempotent repost (same id + payload) | pending |
| Conflict (same id + different payload) | pending |
| Invalid token | pending |
| Invalid `source_service` | pending |

Reproduce from the repository root:

```powershell
$env:MAILER_IMAGE_PLATFORM = 'linux/amd64'; .\scripts\release-smoke.ps1
$env:MAILER_IMAGE_PLATFORM = 'linux/arm64'; .\scripts\release-smoke.ps1
```

```bash
MAILER_IMAGE_PLATFORM=linux/amd64 bash scripts/release-smoke.sh
MAILER_IMAGE_PLATFORM=linux/arm64 bash scripts/release-smoke.sh
```

## Known limitations

- SQLite deployment assumes a single node / single replica.
- Admin UI remains disabled by default, internal-network-only, and experimental.
- `202 Accepted` means the request was persisted, not that provider delivery
  completed.
- Delivery is at-least-once. If the service stops after provider send succeeds
  but before the database records delivery, the same mail may be sent again.
- Webhook endpoints are optional per tenant (one URL per tenant in this release).
- Consumer SDK status polling helpers are not yet included (HTTP GET is available;
  SDK Phase 2 follow-up).
- No template engine.
- No full multi-recipient support.

## Related evidence

- [v0.9.1 release record](v0.9.1.md)
- [Release notes checklist](../ops/release-notes-checklist.en.md)
- [GHCR image publish](../ops/ghcr-image-publish.en.md)
- [Release artifact verification](../ops/release-artifact-verification.en.md)
- [SQLite disk and retention](../ops/sqlite-disk-and-retention.en.md)
