# Amane Mailer v1.1.0 Release Record

Value-free release evidence for OSS consumers. No credentials, recipient addresses,
tokens, raw logs, or host-specific private paths.

This file is the release record for the `v1.1.0` minor release.
Tag, Docker, NuGet, and the GitHub Release fields below start as `_pending_` until
publish and smoke complete.

## Source

- Git tag: `v1.1.0`
- Annotated tag object: `_pending_`
- Tag target commit: `_pending_`
  (`origin/main` squash merge for the promote PR)
- GitHub release: <https://github.com/kooiei-in4a/amane-mailer/releases/tag/v1.1.0>
- Docs / version prep PR: `_pending_` (merged to `develop`)
- Main promotion PR: `_pending_` (merged to `main`)
- Post-promote sync: `_pending_`

## Positioning

- Minor release on the 1.x line. Ships ACS Delivery Report bounce ingestion
  (Storage Queue Pull), tenant suppressions, setup entry / diagnostic CLIs, and
  production deploy wiring for setup modes 4 and 5.
- **Additive HTTP contract change.** Consumers may observe the new delivery
  error code `RECIPIENT_SUPPRESSED` on status / attempts; request schemas are
  unchanged. `Amane.Mailer.Contracts` and OpenAPI `info.version` move to
  `1.1.0`.
- Requires SQLite migration `011_bounce_ingestion.sql` (not present in 1.0.x).
  Backup before upgrade; downgrade to 1.0.x without a data migration path is
  not supported once 011 has been applied.
- Event Grid Push webhook (#304) and Consumer “bounced” notification
  (ADR 0020 D-11) remain out of scope.

## Included changes since v1.0.1

### Bounce ingestion / suppression

- **#301** — Migration 011: `provider_event_inbox`, `bounce_events`,
  `mail_suppressions`.
- **#302** — Ingestion worker: parse ACS Delivery Reports, correlate, record
  bounce history, upsert suppressions.
- **#303** — Send-time suppression with `RECIPIENT_SUPPRESSED` and
  `mail_suppressed_sends_total`.
- **#305** — Azure Storage Queue Pull transport
  (`MAILER_BOUNCE_INGESTION=queue`).
- **#306** — Admin bounce visibility, suppressions list (view-only), bounce
  runbooks / metrics.
- **#400** — `db suppressions remove` CLI with audited removal.

### Setup / operations

- **#424** — Single setup entry guide (JA/EN) with mode taxonomy.
- **#425** — Read-only `setup doctor`.
- **#426** — Staging-only `admin provider test-acs-send`.
- **#427** — Read-only `setup check-event-grid`.
- **#428** — Staging-only `setup verify-delivery-report`.
- **#435** — Production-confirmed `register-acs` (mode 4 Available).
- **#436** — Deploy compose bounce Queue wiring (mode 5 Available).
- **#437** — `setup-entry-guide` marked `implemented`.

### Admin reliability

- **#391** — Monotonic / interval-throttled session touch; absolute cookie
  lifetime; idle expiry from `admin_sessions` only.

### Contracts / OpenAPI

- `Amane.Mailer.Contracts` and OpenAPI `info.version` aligned on `1.1.0`.
- Additive delivery error code `RECIPIENT_SUPPRESSED` (#303).
- Migration `011_bounce_ingestion.sql` required for bounce / suppression tables.

### Out of scope (explicit)

- Event Grid Push webhook (#304).
- Consumer bounced notification (ADR 0020 D-11).
- Azure resource auto-creation.
- Real bounce as a normal setup completion criterion.

## Draft GitHub Release notes

Mirror the `CHANGELOG.md` 1.1.0 section after the promote PR merges and artifacts
are published.

### Artifact status

- Git tag / annotated tag object: `_pending_`
- Docker image URL / digest: `_pending_`
- NuGet package URL and symbol verification: `_pending_`
- Release-image smoke: `_pending_`

## Docker

- Image: `ghcr.io/kooiei-in4a/amane-mailer:v1.1.0`
- Index digest: `_pending_`
- Runtime manifest digest (`linux/amd64`): `_pending_`
- Runtime manifest digest (`linux/arm64`): `_pending_`
- Attestation manifest digest (`linux/amd64`): `_pending_`
- Attestation manifest digest (`linux/arm64`): `_pending_`
- Immutable tag: `_pending_`
- Immutable tag digest: `_pending_`
- Target: `linux/amd64` and `linux/arm64` (expected multi-arch after publish)
- OCI labels: `_pending_`
- Publish workflow: `_pending_`

## NuGet

- Package: `Amane.Mailer.Contracts`
- Version: `1.1.0`
- Package URL: <https://www.nuget.org/packages/Amane.Mailer.Contracts/1.1.0>
- Listed: `_pending_`
- SourceLink / repository commit: `_pending_`
- Service release commit (`v1.1.0` tag on `main`): `_pending_`
- Publish workflow: `_pending_`

### Symbol package

- Generation: `_pending_`
- Push: `_pending_`
- Availability: `_pending_`
- Indexing / debugging: `_pending_`

## Release image smoke

Clean-state pull and automated smoke of the public release runtime path via
[`scripts/release-smoke.sh`](../../scripts/release-smoke.sh). Runbook:
[Clean-state release image smoke](../ops/release-image-smoke.md).

| Check | linux/amd64 | linux/arm64 |
|-------|-------------|-------------|
| Image pull | `_pending_` | `_pending_` |
| `/healthz` 200 | `_pending_` | `_pending_` |
| `/readyz` 200 | `_pending_` | `_pending_` |
| POST accepted | `_pending_` | `_pending_` |
| Mailpit delivery | `_pending_` | `_pending_` |
| Idempotent repost | `_pending_` | `_pending_` |
| Conflict 409 | `_pending_` | `_pending_` |
| 401 / 403 | `_pending_` | `_pending_` |

- Date (JST): `_pending_`
- Environment notes: `_pending_`
- Image digest used: `_pending_`

## Known limitations

- Publish workflow hardening (#23) remains open. Tag/version consistency, safe
  input handling, `release` environment alignment, and SBOM / provenance /
  digest tracking are verified manually for this release rather than guaranteed
  by automation.
- Setup-entry soft residual: final verification against the published `v1.1.0`
  image is completed after publish / smoke (tracked under #424).
