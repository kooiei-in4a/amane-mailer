# Amane Mailer v1.0.0 Release Record

Value-free release evidence for OSS consumers. No credentials, recipient addresses,
tokens, raw logs, or host-specific private paths.

This file is the release record draft for the `v1.0.0` first stable release.
Tag, Docker, NuGet, the GitHub Release, and public release-image smoke
(`linux/amd64` and `linux/arm64`) are filled in after publish.

## Source

- Git tag: `v1.0.0`
- Annotated tag object: `51bed60f195b63b9c9b677c0b541b3e08c7e2518`
- Tag target commit: `8a0d13e0fe7e138a5af6b12c5ca1256532319811`
  (`origin/main` squash merge for PR #393)
- GitHub release: <https://github.com/kooiei-in4a/amane-mailer/releases/tag/v1.0.0>
- Docs / version prep PR: <https://github.com/kooiei-in4a/amane-mailer/pull/392>
  (merged to `develop`)
- Main promotion PR: <https://github.com/kooiei-in4a/amane-mailer/pull/393>
  (merged to `main`)
- CodeQL ReDoS fix before promote: <https://github.com/kooiei-in4a/amane-mailer/pull/394>
- Post-promote sync: <https://github.com/kooiei-in4a/amane-mailer/pull/395>

## Positioning

- First stable (`1.0.0`) release: formal GA declaration plus stabilization of
  the post-v0.9.2 `develop` line.
- Public HTTP contract and `Amane.Mailer.Contracts` follow semver
  backward-compatibility guarantees from this release onward.
- Open P1 follow-ups #388 / #389 / #390 are explicitly **not** release blockers;
  they target 1.0.x.

## Included changes since v0.9.2

### Reliability / security

- Reject invalid UTF-8 request bodies with `400` (#343).
- Reject Admin `ALLOW_HTTP` outside Development; register webhook client when
  worker is disabled (#341).
- Classify schema probe failures separately from `schema_not_ready` (#342).
- Dispose SQLite connections on open / PRAGMA init failure (#344).
- Judge webhook delivery from response headers without buffering the body (#370).

### Operations / configuration

- Strict boolean and port parsing (#358).
- Mailpit SMTP host/port startup validation when in use (#356).
- Centralized startup options validation catalog (#351).
- Rename webhook reconcile batch size to `ReconcileBatchSize` with deprecated
  `BatchClaimSize` alias (#353).

### Admin / Worker

- Require tenant scope on Admin mail attempt listing (#357).
- Await Admin credential sync before route mapping (#350).
- Admin extensions and mail-request endpoint module splits (#349, #348).
- Worker `InflightTracker` async signal (#354).
- CLI Ctrl+C cancellation for long-running commands (#347).

### Quality / SDK / docs

- MailRequest field-inventory CI gate (#352).
- Formatter / staged analyzer gates (#359).
- Python / TypeScript SDK `scheduled_at` builders (#346).
- ADR 0016–0019 (internal typing, webhook first-wins, sequential concurrency
  deferral, SQLite single-process boundaries).

### Contracts / OpenAPI

- `Amane.Mailer.Contracts` and OpenAPI `info.version` aligned on `1.0.0`.
- OpenAPI documents strict UTF-8 rejection for create / reschedule bodies.
- No new consumer DTO fields removed; no new SQL migration in this release.

## Draft GitHub Release notes

Mirror the `CHANGELOG.md` 1.0.0 section after the promote PR merges and artifacts
are published.

### Artifact status

- Git tag / annotated tag object: published; recorded under [Source](#source)
- Docker image URL / digest: **pending** — GHCR publish blocked by repeated GitHub Actions internal errors (runs 30157214417 / 30157822106 / 30157928633). Retry required.
- NuGet package URL and symbol verification: published; recorded under [NuGet](#nuget)
- Release-image smoke: pending until GHCR image exists (`linux/amd64` and `linux/arm64`)

## Docker

- Image: `ghcr.io/kooiei-in4a/amane-mailer:v1.0.0` (pending publish)
- Index digest: _pending_
- Runtime manifest digest (`linux/amd64`): _pending_
- Runtime manifest digest (`linux/arm64`): _pending_
- Attestation manifest digest (`linux/amd64`): _pending_
- Attestation manifest digest (`linux/arm64`): _pending_
- Immutable tag: _pending_ (`sha-<git-sha>`)
- Target: `linux/amd64` and `linux/arm64` (expected multi-arch, same as v0.9.2)
- Publish workflow: _pending_

## NuGet

- Package: `Amane.Mailer.Contracts`
- Version: `1.0.0`
- Package URL: <https://www.nuget.org/packages/Amane.Mailer.Contracts/1.0.0>
- Listed: Yes (`api.nuget.org` flat-container index includes `1.0.0`; `.nupkg` HTTP 200)
- SourceLink / repository commit: `8a0d13e0fe7e138a5af6b12c5ca1256532319811`
- Service release commit (`v1.0.0` tag on `main`):
  `8a0d13e0fe7e138a5af6b12c5ca1256532319811`
- Publish workflow: <https://github.com/kooiei-in4a/amane-mailer/actions/runs/30157213179>
  (`workflow_dispatch` on `v1.0.0`, success)

### Symbol package

- Generation: `Amane.Mailer.Contracts.1.0.0.snupkg` produced in publish workflow
- Push: succeeded via `.nupkg` push (Created) and explicit symbol push (`Conflict` / already exists with `--skip-duplicate`)
- Availability: nuget.org symbol endpoint may still be indexing (`GET .../symbolpackage/.../1.0.0` returned 404 shortly after publish); re-check later
- Indexing / debugging: `not verified`

## Release image smoke

Public checklist after publish. Record value-free results only.

| Check | linux/amd64 | linux/arm64 |
|-------|-------------|-------------|
| Image pull | _pending_ | _pending_ |
| `/healthz` 200 | _pending_ | _pending_ |
| `/readyz` 200 | _pending_ | _pending_ |
| POST accepted | _pending_ | _pending_ |
| Mailpit delivery | _pending_ | _pending_ |
| Idempotent repost | _pending_ | _pending_ |
| Conflict 409 | _pending_ | _pending_ |
| 401 / 403 | _pending_ | _pending_ |

- Date (JST): _pending_
- Environment notes: _pending_
- Image digest used: _pending_

## Operational notes (release checklist)

- `202 Accepted` means Mailer persisted the request; it is not provider delivery
  completion.
- Delivery is at-least-once.
- SQLite deployments remain single-node / single-replica (ADR 0019).
- Admin UI stays disabled by default, internal-network, experimental.
- Upgrade: no new SQL migration beyond v0.9.2 migration 010; back up DB and
  tenant config before upgrade.
- ACS live sending still requires explicit ACS config, secret file in
  Staging/Production, `live_sending=true` tenant, and approved sender/domain.
